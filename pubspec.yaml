name: dub_app
description: Extract speech from videos and audio with timestamps
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.0.3
  http: ^1.1.0
  shared_preferences: ^2.2.0
  provider: ^6.1.1
  path_provider: ^2.1.1
  permission_handler: ^11.0.1
  ffmpeg_kit_flutter_audio: ^6.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
